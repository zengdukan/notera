# Notera 项目协作规则

## 实施计划

- 所有实施计划必须使用中文编写。
- 任务按完整、可独立测试的功能模块划分，不拆成 2–5 分钟的微步骤。
- 每个功能模块任务集中说明目标、功能逻辑、关键接口、涉及文件、单元测试、精确测试命令和完成后的提交。
- 测试与实现属于同一个功能任务；不要把“编写失败测试、运行失败测试、编写实现、运行成功测试”分别列为独立计划步骤。
- 计划生成后只进行一次快速一致性检查，不创建单独的计划审核任务，也不进行重复自审。
- 当前阶段不把同步协议、同步引擎、云端 API、同步 Outbox、同步冲突或远端附件状态纳入实施计划。

## 实施与验证

- 优先关注功能逻辑代码和单元测试。
- 实施过程中只运行当前功能模块相关的单元测试。
- 每完成一个完整功能模块提交一次；不要按测试、实现或验证步骤分别提交。
- 不进行逐任务代码审核、逐任务规格审核、额外审核代理或其他重复审核。
- 不在每个功能模块后重复运行全量 lint、typecheck、build 或全部测试。
- 所有模块完成后只执行一次必要的最终验证：运行相关单元测试全集，并按本次改动的实际需要运行 typecheck、lint 或 build。
- 验证失败时针对失败原因修复并复测；未受影响且已经通过的检查不重复运行。
